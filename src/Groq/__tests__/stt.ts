// const { stt } = require("../stt");
// const fs = require("fs");

// describe("STT function", () => {
//     it("should return transcription text from the file", async () => {
//         const filePath = "./src/Groq/__tests__/Recording.m4a";
//         const file = new File([fs.readFileSync(filePath)], "Recording.m4a", {
//             type: "audio/m4a",
//         });

//         const transcriptionText = await stt(file);

//         expect(transcriptionText).toBe("Yo, what is up, bro?");
//     });
// });
